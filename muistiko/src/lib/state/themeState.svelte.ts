/* <import type {Theme}  from "./gameState.svelte";

export const themeSetting: Record<Theme, {
  music: string,
  colors: {
    header: string
    footer: string
    card: string
    buttons: string
    background: string
    text: string
  }
}> =  */